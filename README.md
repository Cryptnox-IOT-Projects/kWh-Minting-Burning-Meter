# kWh-Minting-Burning-Meter
Software &amp; Hardware to create a Kilowatts per Hour Meter that mints (production) or burns (consumption) a corresponding kWh token using a Cryptnox Card as hardware wallet
